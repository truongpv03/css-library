name: Build & Release CSS Library

on:
  push:
    tags:
      - "v*" # Chạy khi có tag mới dạng v1.0, v2.0, vX.Y

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🔧 Thiết lập Node.js
        uses: actions/setup-node@v18
        with:
          node-version: 18

      - name: 📦 Cài đặt dependencies
        run: |
          npm install clean-css-cli -g

      - name: 🚀 Build CSS
        run: |
          mkdir -p dist
          cleancss -o dist/css-library.min.css src/main.css

      - name: 📦 Upload file build
        uses: actions/upload-artifact@v3
        with:
          name: css-library
          path: dist/css-library.min.css

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 📥 Tải file đã build
        uses: actions/download-artifact@v3
        with:
          name: css-library
          path: dist/

      - name: 🚀 Tạo GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: dist/css-library.min.css
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
